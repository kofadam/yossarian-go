version: '3.3'

services:
  yossarian-go:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - ADMIN_PASSWORD=admin123
      - OIDC_ENABLED=false
    restart: unless-stopped